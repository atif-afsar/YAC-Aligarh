# Frontend chat API URL (set at build time for production)
# Local dev: leave unset — Vite proxies /api/chat to the local backend on :3000
# Production: your deployed Vercel backend, e.g.
# VITE_AI_ASSISTANT_API_URL=https://your-project.vercel.app/api/chat
